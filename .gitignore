# Sensitive config with credentials
config.ini

# Output and cache files (generated — not source)
*_cache.json
Crossref_AuthorMetadata.json

# Input data (may contain sensitive researcher data)
*.xlsx

# Python
__pycache__/
*.pyc
*.pyo
.env
venv/
.venv/
